const restar = (num1, num2) => {
    const resta = num1 - num2;
    return resta;
}

restar(3, 2);
restar(10, 5.5);
restar(3, 5);

console.log("El resultado de la resta es: " + restar(3, 2));
console.log("El resultado de la resta es: " + restar(10, 5.5));
console.log("El resultado de la resta es: " + restar(3, 5));